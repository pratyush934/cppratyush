#include <iostream>
using namespace std;

typedef long long ll;

// Function to find the value of the last set bit
ll lastSetBit(ll n) {
    return (n & -n);
}

int main() {
    ll n = 15; // Example number
    cout << "Last set bit of " << n << " is: " << lastSetBit(n) << endl;
    return 0;
}
